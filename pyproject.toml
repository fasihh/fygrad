[project]
name = "fygrad"
version = "0.1.0"
description = "Minimal auto-gradient library using numpy/cupy"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "build>=1.4.3",
    "numpy>=2.4.4",
    "twine>=6.2.0",
]
